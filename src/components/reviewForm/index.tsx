import React from "react";
import { MovieT } from "../../types/interfaces";

const ReviewForm: React.FC<MovieT> = (props) => {
  return <h3>Placeholder for web form</h3>;
};

export default ReviewForm;
